{
  "version": "2.0.0",
  "tasks": [
    {
      // Compila el paquete del avatar antes de lanzarlo con F5.
      "label": "build-avatar",
      "type": "shell",
      "command": "npx tsc --build",
      "options": { "cwd": "${workspaceFolder}/packages/ui-avatar" },
      "problemMatcher": ["$tsc"],
      "presentation": { "reveal": "silent", "clear": true }
    }
  ]
}
